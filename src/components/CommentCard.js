import styled from "styled-components"

const Ul = styled.ul`

`
const CommentCard = ({cards}) => {
    return (
        <Ul>
            {cards.imageUrl}
            
        </Ul>

    )
}






export default CommentCard
